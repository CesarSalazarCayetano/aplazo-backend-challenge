# First Steps
1. Clone the repository from the fork feature/Cesar_Salazar
2. Install the dependencies with maven clean install
2. The data BD depends of docker-compose.yml file
2.1 Run docker-compose.yml
3. Start the app